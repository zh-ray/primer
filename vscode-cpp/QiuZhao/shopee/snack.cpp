#include<iostream>
#include <vector>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> steps;
    for (size_t i = 0; i < n; i++)
    {
        cin >> steps[i];
    }

}